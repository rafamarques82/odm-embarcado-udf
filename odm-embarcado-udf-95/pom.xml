<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>br.com.itau.odm</groupId>
    <artifactId>embarcado-95</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>ODM Embarcado - Generic UDF (ODM 9.5)</name>
    <description>Generic ODM UDF for Spark — IBM ODM 9.5.0.1</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <spark.version>3.3.0</spark.version>
        <gson.version>2.10.1</gson.version>
        <aws.sdk.version>1.12.529</aws.sdk.version>
        <junit.version>5.10.1</junit.version>
        <mockito.version>5.8.0</mockito.version>
        <odm.version>9.5.0.1</odm.version>
    </properties>

    <dependencies>
        <!-- Spark Core (provided) -->
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-core_2.12</artifactId>
            <version>${spark.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql_2.12</artifactId>
            <version>${spark.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Gson -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson.version}</version>
        </dependency>

        <!-- AWS SDK S3 -->
        <dependency>
            <groupId>com.amazonaws</groupId>
            <artifactId>aws-java-sdk-s3</artifactId>
            <version>${aws.sdk.version}</version>
        </dependency>

        <!-- Kafka (para KafkaMetrics) -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
            <version>3.3.1</version>
        </dependency>

        <!-- IBM ODM 9.5 — engine -->
        <dependency>
            <groupId>com.ibm.rules</groupId>
            <artifactId>jrules-engine</artifactId>
            <version>${odm.version}</version>
            <scope>system</scope>
            <systemPath>${project.basedir}/lib/jrules-engine.jar</systemPath>
        </dependency>

        <!-- IBM ODM 9.5 — execution (RES) -->
        <dependency>
            <groupId>com.ibm.rules</groupId>
            <artifactId>jrules-res-execution</artifactId>
            <version>${odm.version}</version>
            <scope>system</scope>
            <systemPath>${project.basedir}/lib/jrules-res-9.5.0.1-execution.jar</systemPath>
        </dependency>

        <!-- IBM ODM 9.5 — session Java -->
        <dependency>
            <groupId>com.ibm.rules</groupId>
            <artifactId>jrules-res-session</artifactId>
            <version>${odm.version}</version>
            <scope>system</scope>
            <systemPath>${project.basedir}/lib/jrules-res-9.5.0.1-session-java.jar</systemPath>
        </dependency>

        <!-- IBM License Metric Logger -->
        <dependency>
            <groupId>com.ibm.license</groupId>
            <artifactId>license-metric-logger</artifactId>
            <version>2.1.2</version>
            <scope>system</scope>
            <systemPath>${project.basedir}/lib/license_metric_logger_2.1.2.202009181115.jar</systemPath>
        </dependency>

        <!-- Jakarta EE Connector API (requerido pelo jrules-res-execution 9.5) -->
        <dependency>
            <groupId>jakarta.resource</groupId>
            <artifactId>jakarta.resource-api</artifactId>
            <version>2.1.0</version>
        </dependency>

        <!-- JUnit 5 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Mockito -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Maven Surefire Plugin - JUnit 5 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.2</version>
                <configuration>
                    <argLine>--add-opens java.base/java.util=ALL-UNNAMED --add-opens java.base/java.lang=ALL-UNNAMED</argLine>
                    <additionalClasspathElements>
                        <additionalClasspathElement>${project.basedir}/exemplo/testeFinanciamento.jar</additionalClasspathElement>
                        <additionalClasspathElement>${project.basedir}/exemplo/xom-classes</additionalClasspathElement>
                    </additionalClasspathElements>
                </configuration>
            </plugin>

            <!-- Maven Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>

            <!-- Maven AntRun Plugin - Descompacta JARs do ODM 9.5 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>unpack-odm-jars</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <echo message="Descompactando JARs do ODM 9.5..."/>
                                <unzip src="${project.basedir}/lib/jrules-engine.jar"
                                       dest="${project.build.directory}/classes"
                                       overwrite="true"/>
                                <unzip src="${project.basedir}/lib/jrules-res-9.5.0.1-execution.jar"
                                       dest="${project.build.directory}/classes"
                                       overwrite="true"/>
                                <unzip src="${project.basedir}/lib/jrules-res-9.5.0.1-session-java.jar"
                                       dest="${project.build.directory}/classes"
                                       overwrite="true"/>
                                <unzip src="${project.basedir}/lib/license_metric_logger_2.1.2.202009181115.jar"
                                       dest="${project.build.directory}/classes"
                                       overwrite="true"/>
                                <echo message="JARs do ODM 9.5 descompactados com sucesso!"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Maven Shade Plugin — fat JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                            <createDependencyReducedPom>true</createDependencyReducedPom>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>br.com.itau.odm.embarcado.GenericODMUDF</mainClass>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
